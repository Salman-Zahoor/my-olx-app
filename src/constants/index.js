import { Dimensions } from "react-native"

const vh=Dimensions.get("window").height
const vw=Dimensions.get("window").width

const globaltextcolor="black"
const globalbackground="white"

export{
    globaltextcolor,
    globalbackground,
    vh,
    vw
}